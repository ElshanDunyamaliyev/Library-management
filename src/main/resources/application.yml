spring:
    application:
        name: library
    datasource:
        url: jdbc:postgresql://localhost:5432/postgres
        password: postgres
        username: postgres
    jpa:
#        hibernate:
#            ddl-auto: create
        properties:
            hibernate:
                format_sql: true
        show-sql: true
    liquibase:
        change-log: liquibase/changelog-master.yml
